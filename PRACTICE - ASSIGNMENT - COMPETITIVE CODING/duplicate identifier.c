#include<stdio.h>
int main()
{
    int n=10,i, a[n];
    int visited[10] = {0}, count =0;

    for(i=0; i<n; i++)
    {
        printf("Enter the value of a[%d]: ");
        scanf("%d",&a[i]);
    }

    

}